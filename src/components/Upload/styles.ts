// Shared sizing for the upload form: every field and the card that holds them
// stay on the same responsive width ladder.
export const fullWidth = {
  minWidth: {
    xs: '100%',
    sm: '90%',
    md: '70%',
    lg: '60%',
  },
} as const;
